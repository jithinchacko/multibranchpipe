pipeline {
    agent { label 'master'}
    stages {
        stage('scm') {
            steps {
                git 'https://github.com/jithinchacko/mavenjavacode.git'
            }
        }
        stage('build') {
            steps{
                sh 'mvn package'
            }
        }
        stage('postbuild') {
            steps{
                archiveArtifacts "target/*.jar"
            }
        }
    }
}